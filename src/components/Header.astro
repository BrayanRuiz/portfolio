---
---
<header class="fixed top-0 left-0 right-0 z-50 bg-black/50 backdrop-blur-md px-6 py-4">
    <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-2xl font-bold">
            <span class="text-accent">~/</span>portfolio
        </h1>
        <nav>
            <ul class="flex gap-6">
                <li><a href="#inicio" class="nav-link hover:text-accent transition-colors">Inicio</a></li>
                <li><a href="#proyectos" class="nav-link hover:text-accent transition-colors">Proyectos</a></li>
                <li><a href="#sobre-mi" class="nav-link hover:text-accent transition-colors">Sobre mí</a></li>
                <li><a href="#certificaciones" class="nav-link hover:text-accent transition-colors">Certificaciones</a></li>
                <li><a href="#skills" class="nav-link hover:text-accent transition-colors">Skills</a></li>
            </ul>
        </nav>
    </div>
</header>

<script>
    document.querySelectorAll('.nav-link').forEach(link => {
        link.addEventListener('click', (e) => {
            e.preventDefault();
            const href = link.getAttribute('href');
            if (!href) return;
            
            const targetId = href.substring(1);
            const targetElement = document.getElementById(targetId);
            
            if (targetElement) {
                const headerOffset = 80;
                const elementPosition = targetElement.getBoundingClientRect().top;
                const offsetPosition = elementPosition + window.pageYOffset - headerOffset;

                window.scrollTo({
                    top: offsetPosition,
                    behavior: 'smooth'
                });
            }
        });
    });
</script>

<style>
    .nav-link {
        position: relative;
    }

    .nav-link::after {
        content: '';
        position: absolute;
        width: 0;
        height: 2px;
        bottom: -2px;
        left: 0;
        background-color: var(--accent-color);
        transition: width 0.3s ease;
    }

    .nav-link:hover::after {
        width: 100%;
    }
</style>
